// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Nếu cần thêm rewrites/redirects có thể thêm ở đây.
  // Không thêm key experimental: { appDir: true } theo yêu cầu.
};

module.exports = nextConfig;
