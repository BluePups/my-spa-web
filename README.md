# My SPA Website — Thẩm mỹ viện (Next.js + MUI v7 + TypeScript)

## Mục tiêu
Project demo website thẩm mỹ viện, sử dụng Next.js (App Router), TypeScript, Material UI v7, MUI X Date Pickers (AdapterDayjs). Dữ liệu lưu tại local state / console (không dùng DB).

## Cấu trúc
Xem cây thư mục trong đề bài.

## Cài đặt & chạy
1. Cài dependencies:
   ```bash
   npm install
